# govulncheck vulnerability allowlist.
# One Go vuln ID (GO-YYYY-NNNN) per line. Lines starting with # are
# ignored. Add an entry only when the vulnerability is in transitive
# code we don't actually invoke. Document the rationale in the commit
# message that adds it.

# smallstep/certificates SCEP UpdateReq unauthenticated certificate
# issuance. Reachable transitively via caddy/v2/modules/standard's
# acme_server, but we don't use SCEP. Will be fixed when Caddy bumps
# smallstep/certificates to v0.30.0+.
GO-2026-4775
